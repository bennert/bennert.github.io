name: Generate Driving Schedule

on:
  workflow_dispatch:  # Handmatig activeren
  schedule:
    - cron: '0 6 * * 1'  # Elke maandag om 6:00 UTC

jobs:
  generate-schedule:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install reportlab markdown2
        
    - name: Create driving schedule
      run: |
        python create_driving_schedule.py
      env:
        MAPS_API_KEY: ${{ secrets.MAPS_API_KEY }}
        SPORTLINK_TOKEN_LIST: ${{ secrets.SPORTLINK_TOKEN_LIST }}
        
    - name: Convert to PDF
      run: |
        python convert_driving_schedule_to_pdf.py
        
    - name: Upload PDF as artifact
      uses: actions/upload-artifact@v4
      with:
        name: driving-schedules-${{ github.run_number }}
        path: docs/Handbal/*.pdf
        retention-days: 90

    - name: Send email with PDF attachments
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 465
        secure: true
        username: ${{ secrets.EMAIL_USERNAME }}
        password: ${{ secrets.EMAIL_PASSWORD }}
        subject: Driving Schedule - ${{ github.run_number }}
        to: ${{ secrets.EMAIL_TO }}
        from: ${{ secrets.EMAIL_FROM }}
        body: |
          Nieuw rijschema gegenereerd!
          
          Zie de bijgevoegde PDF bestanden.
          
          Workflow run: ${{ github.run_number }}
          Datum: ${{ github.event.repository.updated_at }}
        attachments: docs/Handbal/*.pdf